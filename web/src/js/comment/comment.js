var cgi = require('../common/cgi').comment;
var tmpl = {
	list : require('../../tpl/comment/list.ejs')
};

var Comment = {}

Comment.init = function(id){
	// articleList.init(id,cgi,tmpl);
	// articlePost.init(id,cgi,tmpl);
}